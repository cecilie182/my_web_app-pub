<div id="problem1" class="container">

    <div id="solution">
        <div class="pricing-header px-3 py-3 pt-md-5 pb-md-4 mx-auto text-center">
            <h1 class="display-4">Problem #1</h1>
            <p class="lead">The result obtained by placing the kids of levels
                <b><{% for level in levels %}{{ level }}{% if not loop.last %}, {% endif %}{% endfor %}></b>
                in two teams of equal size with the total skill level being as even as possible.</p>
        </div>

        <div class="row row-cols-1 row-cols-md-2 mb-2 text-center">
            <div class="col">
                <div class="card mb-4 shadow-sm">
                    <div class="card-header">
                        <h4 class="my-0 fw-normal">TEAM 1</h4>
                    </div>
                    <div class="card-body">
                        <h2 class="card-title">Level : {{ level1 }}</h2>
                        {% for child in team1 %}
                            {{ child }}{% if not loop.last %}, {% endif %}
                        {% endfor %}
                    </div>
                </div>
            </div>

            <div class="col">
                <div class="card mb-4 shadow-sm">
                    <div class="card-header">
                        <h4 class="my-0 fw-normal">TEAM 2</h4>
                    </div>
                    <div class="card-body">
                        <h2 class="card-title">Level : {{ level2 }}</h2>
                        {% for child in team2 %}
                            {{ child }}{% if not loop.last %}, {% endif %}
                        {% endfor %}
                    </div>
                </div>
            </div>
        </div>
    </div>
    <p>Try with another group of kids : {{ form(form) }}</p>
</div>
